# Copy to .env and fill in as needed. Never commit .env.

# Optional: Census API key (recommended; raises rate limits).
# Request one free at https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=
